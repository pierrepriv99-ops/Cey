<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cryos.vault">

    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS" />
    
    <application android:label="@string/app_name">
        
        <!-- KeyStore Service using TEE -->
        <service
            android:name=".KeyStoreService"
            android:exported="false"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <intent-filter>
                <action android:name="android.app.admin.KeyChainCallback" />
            </intent-filter>
        </service>
        
        <!-- Biometric Auth Activity -->
        <activity
            android:name=".BiometricAuthActivity"
            android:exported="false"
            android:theme="@style/Theme.CryOS" />
            
    </application>
</manifest>